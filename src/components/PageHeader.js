import React from "react";
import NavBar from "./NavBar";

const PageHeader = () => {
  return (
    <div class="hero-head">
      <header class="navbar">
        <div class="container">
          <NavBar />
        </div>
      </header>
    </div>
  );
};

export default PageHeader;
